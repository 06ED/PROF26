name: ai_notes_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  ai_notes_query:
    git:
      url: https://github.com/diquoks/PROF26
      ref: ai-notes/new/query
  ai_notes_uikit:
    git:
      url: https://github.com/diquoks/PROF26
      ref: ai-notes/new/uikit
  dio: ^5.9.0
  get: ^4.7.3
  flutter_screenutil: ^5.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
